# Clage Cook OSS — Vision & 実装方針

## コンセプト
「1つのAIに聞く」から「**複数AIに議論させ、統合役が最良の答えを出す**」へ。
Clage Cook は4つのAI(Claude / Gemini / ChatGPT / Grok)に同じ質問を並列で投げ、
統合役AIが一致点・相違点を踏まえて1つの回答にまとめる **AI会議アプリ**。

OSS版は各社の公式API(BYOK)で誰でも動かせ、Flutterで **全プラットフォーム** に届ける。

## なぜ作るか
- 単一AIの偏り・見落としを、複数AIの合議で補う。
- 統合役が最良の回答を生成(`!debate` で相互批評ラウンドも)。
- **キー1つで動く**。キーが無くてもモックで会議フローを体験できる。

## 設計方針
- **プロバイダ抽象化**: `backend/providers/` で各社API(Anthropic / OpenAI / Gemini /
  xAI)とモックを同一インターフェースに。`orchestrator` は接続方式を一切知らない。
  実↔モックの切替は `config.py` だけで完結する。
- **バックエンド共通 + フロント全PF**: FastAPI 1つ + Flutter(Web / Windows / macOS /
  Linux / Android / iOS)。UIはFlutterだけ書けば全PFに乗る。
- **コアは合議オーケストレーション**。接続方式やプラットフォームは手段であり、
  価値の中心はここ。

## ロードマップ
1. [x] モックで4AI会議 + 統合(SSEストリーミング)
2. [ ] 各社公式APIプロバイダ(Anthropic / OpenAI / Gemini / xAI)
3. [ ] `!debate`(相互批評ラウンド) / 品質tier(high・low)
4. [ ] web検索・画像生成(各社API)
5. [ ] 会話履歴・設定画面・全PFビルド
6. [ ] デモ動画 + ポートフォリオ公開

## スコープ / 棲み分け
- OSS版は **各社API直接(BYOK)**。テキスト会議 + 統合が中心。web検索・画像生成は
  各社API実装後に追加する。
- オリジナル(各AIのサブスクCLIを束ねるローカル専用版, `C:\code\ClageCook`)とは
  別物。CLI版は利用規約上OSS化しない。設計知見のみ引き継ぐ。

## ポートフォリオでの見せ場
**「複数AIの回答を統合役がまとめるオーケストレーション設計」**が主役。全PF対応は加点。
デモ動画 + アーキ図で「動く証拠 + 設計の質 + 技術の幅」を同時に伝える。
